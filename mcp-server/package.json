{
  "name": "@hatch/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Hatch — lets Claude Code deploy local apps to your team via the Hatch daemon.",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "hatch-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
