# Copy to .env.local and fill in. Leave blank to run in single-user dev mode
# (the daemon listens only on 127.0.0.1, so this is safe locally).
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
